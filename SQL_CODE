SELECT
	id, account_id, total_amt_usd
FROM
	orders
ORDER BY
	account_id, total_amt_usd DESC;
  
SELECT
  id, account_id, total_amt_usd
FROM
  orders
ORDER BY
  account_id ASC, total_amt_usd DESC;


SELECT
  COUNT(*)
FROM
  birthdate;


SELECT COUNT
  (DISTINCT birthdate)
FROM
  people;
  


  
